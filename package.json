{
  "name": "posintory-monorepo",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"backend,frontend\" -c \"magenta,cyan\" \"cd backend && php artisan serve\" \"cd frontend && npm run dev\"",
    "install:all": "composer install --working-dir=backend && cd frontend && npm install",
    "backend": "cd backend && php artisan serve",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
